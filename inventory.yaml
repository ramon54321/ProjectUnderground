# IP setup in ~.ssh/config
all:
  hosts:
    master-root:
  vars:
    users:
    - name: "ramon"
      groups: "admin"
    packages:
    - name: "tree"
    - name: "git"
    - name: "ranger"
    - name: "unzip"
    - name: "zip"
